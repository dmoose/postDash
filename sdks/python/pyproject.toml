[project]
name = "postDash"
version = "0.1.0"
description = "Python SDK for postDash — real-time event streaming"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
